[project]
name = "nhl-timeouts-replication"
version = "1.0.0"
description = "Replication package: Late, Rare, and Suboptimal — NHL coaches' timeout decisions"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "pandas>=2.2",
    "numpy>=1.26",
    "matplotlib>=3.9",
    "statsmodels>=0.14",
    "scipy>=1.13",
    "tqdm>=4.66",
    "pyarrow>=16",
]
